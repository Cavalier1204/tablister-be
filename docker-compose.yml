services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: tablister
      POSTGRES_PASSWORD: tablister
      POSTGRES_DB: tablister
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
